{
  "name": "js-debug-proxy-requestor",
  "displayName": "js-debug-proxy-requestor",
  "description": "",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.55.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:js-debug-proxy-requestor.getProxy"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "js-debug-proxy-requestor.getProxy",
        "title": "Request CDP Proxy"
      }
    ]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "yarn run lint",
    "test": "node ./test/runTest.js"
  },
  "devDependencies": {
    "@types/vscode": "^1.55.0",
    "@types/glob": "^7.1.3",
    "@types/mocha": "^8.0.4",
    "@types/node": "^12.11.7",
    "eslint": "^7.19.0",
    "glob": "^7.1.6",
    "mocha": "^8.2.1",
    "typescript": "^4.1.3",
    "vscode-test": "^1.5.0"
  }
}
